# Findings

- Generator input contract: first record must be retrieve_asset; second record must contain only place_instance tool calls.
- Fixed workflow requirement: set_support batch, check_support batch, support moves, check_collision, collision moves if needed, recheck as needed, simulate_step, save_scene_usd(include_physics=false).
- Attachment 4fc52384 already had a usable first-two-record seed shape: retrieve_asset followed by place_instance x5. Later original steps were invalid for LoRA because they checked collision before support workflow and saved with include_physics=true.
- The original seed converged under the fixed generator without asset replacement.
- Attachment 17ca090b required seed consolidation: initial IndustrialPart retrieval was empty, but a later supplemental retrieval found Part_with_bbox_meters-95-9ccb8f1c. The consolidated original seed still failed fixed generation with "cannot simulate before scene is collision free".
- Attachment 17ca090b regenerated successfully after replacing the difficult Workbench/Box/IndustrialPart support assets with known convergent same-type assets and spacing Rack/AGV away from the support zones. Final support relations are Box.a.2 -> Workbench.c.9 and IndustrialPart.l.1 -> Conveyor.c.1.
- Attachment a8e76262 required seed consolidation from split place_instance records. The consolidated original seed failed fixed generation with "cannot simulate before scene is collision free", matching the source trace's support/collision oscillation around IndustrialPart.k.17 -> Pallet.a.3.
- Attachment a8e76262 regenerated successfully after replacing with the known convergent robot/pallet/rack same-type asset set from robot_pallet_rack_bee. Final support relations are IndustrialPart.l.22 -> Pallet.a.5 and Box.a.7 -> Rack.a.3.
- Attachment 1006731f required seed consolidation: the part was retrieved and placed later, so the successful part retrieval and placement were merged into the first two seed records. The consolidated original seed converged without asset replacement.
- Attachment 309cf0e2 had a usable first-two-record shape, but the original Rack/Pallet/AGV/Box seed failed fixed generation before simulation because collision_free was not achieved. The source trace also showed support/collision oscillation for the pallet-supported box and no final save.
- Attachment 309cf0e2 regenerated successfully after minimal same-type replacement/adjustment: keep the AGV pickup task structure, use known stable Rack/Pallet/Box geometry, move AGV away from the support zone, and keep two support relations Box.a.8 -> Rack.a.1 and Box.a.8_1 -> Pallet.a.3.
- Attachment 7600f20b required seed consolidation: the source placed Rack/Pallet/Forklift/Box across multiple records, performed collision movement before support workflow, and saved with include_physics=true without a simulate_step record. Consolidating all five placements into record 2 and using the trace's collision-resolved Pallet/Forklift positions allowed the original assets to converge under the fixed generator without replacement.
- Attachment c277c1de already had valid first-two records: retrieve_asset followed by place_instance x6. The original continuation was not LoRA-ready because it checked support before set_support, did not re-check support after registering the relation, skipped simulate_step, and saved with include_physics=true. The original assets converged under the fixed generator without replacement.
- Attachment ec7b6a8d had valid retrieve/place seed material in the first two records. The original continuation was not workflow-ready because it repaired collision before support setup, checked support relations separately, skipped simulate_step, and saved with include_physics=true. The extracted retrieve/place seed converged under the fixed generator without replacement with support relations IndustrialPart.l.22 -> Workbench.c.9 and Box.a.5 -> Workbench.c.9.
- Attachment 631aa342 required seed consolidation from split place_instance records. The consolidated original AGV/conveyor handoff seed failed before simulation because collision_free was not achieved, matching the source trace's support/collision oscillation. It regenerated successfully after minimal same-type replacement using a known stable AGV/conveyor handoff set, with final support relations Box.a.7 -> Pallet.a.1 and IndustrialPart.l.1 -> Conveyor.c.1.
- Attachment 157bef15 had usable first-two records: retrieve_asset followed by place_instance x4. The original continuation was not workflow-ready because it checked collision before registering support, oscillated around Box.a.8 support/collision repairs, skipped simulate_step, and saved with include_physics=true. The original seed also failed fixed generation before simulation with collision_free unresolved, so it was regenerated after same-type replacement using the stable robot_inspection_workstation_3fb set; final support relations are IndustrialPart.l.15 -> Workbench.c.9 and Box.a.2 -> Workbench.c.9.
- Attachment 6371173e required seed consolidation because Rack was placed in record 2 and Pallet/Box/Box/Forklift were placed in record 3. The original continuation was not fully LoRA-ready because it repaired collision before support workflow and saved with include_physics=true. After merging all placements into record 2, the original assets converged under the fixed generator without replacement; final support relations are Box.a.5 -> Rack.a.2 and Box.a.5_1 -> Pallet.a.1.
- Attachment f2264bcc required seed consolidation from two retrieve_asset calls and split placements across records 3-6; the first IndustrialPart retrieval was empty, while the supplemental retrieval returned Part_with_bbox_meters-30-59948bfb used by placement. The original continuation skipped simulate_step and saved with include_physics=true. After merging retrieval/place material into the generator seed, the original assets converged without replacement; final support relations are Box.a.5 -> Conveyor.c.1 and IndustrialPart.l.3 -> Workbench.c.9.
- Attachment 82ae8e56 had valid first-two seed material, but the original continuation checked support before set_support and then oscillated between support and collision moves for IndustrialPart.k.18 on Pallet.a.2 without reaching save. The original seed did not return after several minutes in Isaac check_collision, so it was regenerated after same-type replacement using the stable robot_pallet_rack_bee set; final support relations are IndustrialPart.l.22 -> Pallet.a.5 and Box.a.7 -> Rack.a.3.
- Attachment 064da545 required seed consolidation from split place_instance records and a later Workbench move. The original continuation was not LoRA-ready because it checked support before set_support, interleaved collision/support repairs without the fixed workflow, and did not reach simulate/save. The consolidated original seed did not return after several minutes in Isaac check_collision, so it was regenerated after same-task stable replacement with four required categories only: Conveyor, IndustrialRobot, Workbench, IndustrialPart. Final support relation is IndustrialPart.n.1 -> Workbench.a.10, and the generated workflow passes Isaac collision, support, and simulation checks before save_scene_usd(include_physics=false).
- Attachment 303ae66b required seed consolidation from split place_instance records. The original continuation was not LoRA-ready because it checked collision before support workflow, moved Box around before a formal support check, skipped simulate_step, and saved with include_physics=true. After merging Rack/Pallet/AGV/Box into the second seed record and preserving the trace's collision-resolved Pallet x position, the original assets converged under the fixed generator without replacement. Final support relation is Box.a.2 -> Pallet.a.1, with a support-suggestion move to z=0.2826755383012697 before collision, simulation, and save_scene_usd(include_physics=false).
- Attachment ec81aed3 required seed consolidation from split place_instance records and late-stage moved positions. The original 20-step continuation was not LoRA-ready because it interleaved collision moves before support registration, never called set_support, and did not reach simulate/save. After merging Rack/Pallet/Forklift/Box/Box into the second seed record and preserving later trace positions, the original assets converged under the fixed generator without replacement. Final support relations are Box.c.5 -> Pallet.a.4 and Box.a.2 -> Rack.a.3, followed by support-suggestion moves, collision-suggestion moves, support recheck, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment 7d4c1836 required seed consolidation from split place_instance records. The original 20-step continuation was not LoRA-ready because it spent the trace on collision moves, never registered the IndustrialPart -> Conveyor support relation, and did not reach simulate/save. After merging Rack/Conveyor/IndustrialRobot/IndustrialPart/Pallet into the second seed record and preserving later trace positions, the original assets converged under the fixed generator without replacement. Final support relation is IndustrialPart.l.44 -> Conveyor.b.2, followed by a support-suggestion move, collision-suggestion moves, support recheck, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment 4ddc2385 required seed consolidation from split place_instance records. The original continuation was not LoRA-ready because it checked collision before support workflow, saved with include_physics=true, and skipped simulate_step. After merging Workbench/Rack/Box/IndustrialPart into the second seed record and preserving later collision-cleared positions, the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.l.12 -> Workbench.a.5 and Box.c.6 -> Rack.a.2, followed by support-suggestion moves, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment c6be4ace required seed consolidation from split place_instance records. The original continuation was not LoRA-ready because it checked support before registering support relations, repaired collision before the fixed support workflow, and stopped at set_support without simulate_step/save. The consolidated original seed did not return from Isaac check_support after several minutes, so the final regenerated LoRA candidate uses a stable same-task AGV/conveyor handoff replacement seed while preserving the c6be command semantics. Final support relations are Box.a.7 -> Pallet.a.1 and IndustrialPart.l.1 -> Conveyor.c.1, followed by batch set_support/check_support, collision-suggestion moves, support recheck and support move, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment 03b0b33b required seed consolidation from split place_instance records. The original continuation was not LoRA-ready because it moved objects and checked collision before support registration, delayed set_support until step 17, and stopped after check_collision without simulate_step/save. The consolidated original seed did not return from Isaac check_support after several minutes, so the final regenerated LoRA candidate uses a stable same-task Workbench/Conveyor/Box/IndustrialPart/IndustrialRobot replacement seed while preserving the command semantics. Final support relations are IndustrialPart.l.1 -> Workbench.a.5 and Box.a.5 -> Conveyor.b.3, followed by batch set_support/check_support, support moves, collision-suggestion moves, support recheck and support move, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment cd9a698e required seed consolidation from split place_instance records and a later supplemental Box placement. The original continuation was not LoRA-ready because it repaired collision before support setup, checked only Box.a.8 -> Rack.a.1 while leaving the pallet relation unvalidated, skipped simulate_step, and saved with include_physics=true. After merging Rack/Pallet/Forklift/two Box placements into the second seed record, the original assets converged under the fixed generator without replacement. Final support relations are Box.a.8 -> Rack.a.1 and Box.a.8_1 -> Pallet.a.4, followed by batch set_support/check_support, one support-suggestion move, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment da89fa40 required seed cleanup and consolidation. The first retrieve_asset call had invalid top-level top_k and was excluded; the second retrieve succeeded and was used as the seed retrieve. The original continuation was not LoRA-ready because it repaired collision before support setup, checked only IndustrialPart.n.17 -> Conveyor.b.4 while leaving Box.a.6 -> Workbench.c.9 unvalidated, skipped simulate_step, and saved with include_physics=true. After merging Rack/Conveyor/Workbench/AGV/IndustrialPart/Box placements into the second seed record, the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.n.17 -> Conveyor.b.4 and Box.a.6 -> Workbench.c.9, followed by batch set_support/check_support, one support-suggestion move, collision clear, simulate_step, and save_scene_usd(include_physics=false).
- Attachment e53fa206 required seed consolidation from two retrieve_asset calls and split placement records. The source continuation was not LoRA-ready because it repaired collision before/around the fixed support workflow, split support checks, oscillated part support/collision repairs, used replace_instance, and did not reach simulate_step/save. The consolidated original seed hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task robot loading buffer replacement seed while preserving the user command semantics. Final support relations are IndustrialPart.l.22 -> Pallet.a.5 and Box.a.7 -> Rack.a.3, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 05d4a7d3 required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before setting support, checked support before set_support, checked only partial support after registering relations, oscillated Workbench/IndustrialPart support and collision repairs, and never reached simulate_step/save. The consolidated original seed hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task robot assembly replacement seed while preserving the user command semantics. Final support relations are IndustrialPart.l.1 -> Workbench.a.5 and Box.a.5 -> Conveyor.b.3, followed by batch set_support/check_support, support moves, collision-suggestion moves, support recheck with one support move, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment f36bd3f2 required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before the support workflow, then repeatedly moved the pallet-supported box between support and collision suggestions, and never reached simulate_step/save. The consolidated original seed hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task AGV rack/pallet replacement seed while preserving the user command semantics. Final support relations are Box.a.8_1 -> Pallet.a.3 and Box.a.8 -> Rack.a.1, followed by batch set_support/check_support, one support-suggestion move, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 3ea0d7e3 required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before support setup, set and checked support relations one at a time, skipped simulate_step, and saved with include_physics=true. After merging Rack/Pallet/Forklift/two Box placements into the second seed record and preserving later collision-separated source positions, the original assets converged under the fixed generator without replacement. Final support relations are Box.a.2 -> Pallet.a.1 and Box.a.9 -> Rack.a.3, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 6ed806bd required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it checked support before set_support, skipped simulate_step, and saved with include_physics=true. After merging Conveyor/IndustrialRobot/IndustrialPart/Pallet/Workbench placements into the second seed record and preserving collision-cleared source positions, the original assets converged under the fixed generator without replacement. Final support relation is IndustrialPart.l.1 -> Conveyor.a.2, followed by set_support, check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 159f2125 required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before support setup, then oscillated Workbench/IndustrialPart support/collision checks and never reached simulate/save. The consolidated original seed hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task manual inspection workstation replacement seed preserving Workbench/Rack/IndustrialPart/Box/AGV categories and the source command semantics. Final support relations are IndustrialPart.l.22 -> Workbench.c.9 and Box.a.5 -> Workbench.c.9, followed by batch set_support/check_support, one support-suggestion move, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 378c44ea required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before support setup, checked support relations separately, skipped simulate_step, and saved with include_physics=true. After merging Conveyor/Pallet/Box/IndustrialPart/AGV placements into the second seed record and preserving later source positions, the original assets converged under the fixed generator without replacement. Final support relations are Box.a.6 -> Pallet.a.3 and IndustrialPart.k.17 -> Conveyor.a.2, followed by batch set_support/check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 8d5cda08 required seed consolidation from split place_instance records. The source continuation was not LoRA-ready because it repaired collision before support setup, moved the workbench before formal support checks, skipped simulate_step, and saved with include_physics=true. After merging Workbench/IndustrialPart/Box/IndustrialRobot placements into the second seed record and preserving later source positions, the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.l.22 -> Workbench.b.7 and Box.a.8 -> Workbench.b.7, followed by batch set_support/check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment cdbafda2 is not directly LoRA-ready because the original trace repairs collision before support setup, skips simulate_step, and saves with include_physics=true. After merging Rack/Pallet/two Box/Forklift placements into the second seed record and preserving later source positions, the original assets converged under the fixed generator without replacement. Final support relations are Box.a.1 -> Rack.a.1 and Box.a.7 -> Pallet.a.3, followed by batch set_support/check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 57ddb180 is not directly LoRA-ready because the original trace repairs collision before the support workflow, repeatedly oscillates IndustrialPart.k.17 on Workbench.a.3 between support and collision suggestions, and never reaches simulate_step/save_scene_usd. The consolidated original seed hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task packing/offload replacement seed while preserving the user command semantics. Final support relations are Box.a.5 -> Conveyor.c.1 and IndustrialPart.l.3 -> Workbench.c.9, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment f381e7d7 is not directly LoRA-ready and cannot be used as a generator seed as-is because it contains only retrieve_asset records, with the first IndustrialPart retrieval empty and no place_instance record. The regenerated LoRA candidate therefore uses a stable same-task robot loading buffer replacement retrieve/place seed while preserving the user command semantics. Final support relations are IndustrialPart.l.22 -> Pallet.a.5 and Box.a.7 -> Rack.a.3, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 3ed61ec1 is not directly LoRA-ready because it stops after placing only Conveyor.a.2 and lacks the remaining required placements plus support/collision/simulation/save workflow. The source retrieve results are sufficient, so the fixed generator can use an original-asset consolidated seed placing Conveyor_with_bbox_meters-6-9eec2967, IndustrialRobot_with_bbox_meters-3-6a224e44, Workbench_with_bbox_meters-10-2ee57d66, and Part_with_bbox_meters-28-aa4b3b69. Final support relation is IndustrialPart.l.1 -> Workbench.a.5, followed by set_support, check_support, one support-suggestion move, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment ebae096a is not directly LoRA-ready because it stops after placing only Rack.a.2 and lacks the remaining required placements plus support/collision/simulation/save workflow. The source retrieve results are sufficient, so the fixed generator can use an original-asset consolidated seed placing Rack_with_bbox_meters-1-d18b1c63, Pallet_with_bbox_meters-4-95008ed6, AGV_with_bbox_meters-2-03c38699, and Box_with_bbox_meters-1-ad504646. Final support relation is Box.a.2 -> Pallet.b.1, followed by set_support, check_support, one support-suggestion move, collision-suggestion moves for Rack/Pallet, support recheck, final collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment c459e794 has a valid generator seed shape but is not complete LoRA workflow data because it only includes retrieve_asset and place_instance x5. The original seed hung in Isaac check_support for Box.a.1 -> Pallet.a.5 / Box.a.5 -> Rack.a.3, so the final regenerated LoRA candidate uses a stable same-task forklift replenishment replacement seed while preserving the user command semantics. Final support relations are Box.a.2 -> Pallet.a.1 and Box.a.9 -> Rack.a.3, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment de980da3 is not directly LoRA-ready because it stops after placing only Rack.a.2 while the command needs Conveyor/IndustrialRobot/IndustrialPart/Pallet and Part -> Conveyor support. The source retrieve result has IndustrialPart count 0, so using original assets would make an inconsistent seed. The regenerated LoRA candidate uses a stable same-task conveyor sorting replacement seed while preserving the user command semantics. Final support relation is IndustrialPart.l.1 -> Conveyor.a.2, followed by set_support, check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 864d5e65 is not directly LoRA-ready because it stops after placing only Workbench.c.9 and lacks Rack/Box/IndustrialPart placement plus support/collision/simulation/save workflow. The source retrieve results are sufficient, so the fixed generator can use an original-asset consolidated seed placing Workbench_with_bbox_meters-27-d557a83e, Rack_with_bbox_meters-1-d18b1c63, Box_with_bbox_meters-13-d6d07819, and Part_with_bbox_meters-39-fea24046. Final support relations are IndustrialPart.l.12 -> Workbench.c.9 and Box.c.5 -> Rack.a.2, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 2da8a4e0 is not directly LoRA-ready because it stops after placing only Conveyor.c.1 and lacks Pallet/Box/IndustrialPart/AGV placement plus support/collision/simulation/save workflow. The source retrieve results are sufficient, so the fixed generator can use an original-asset consolidated seed placing Conveyor_with_bbox_meters-0-ada5ace7, Pallet_with_bbox_meters-3-c2d32c78, Box_with_bbox_meters-11-b3231b7d, Part_with_bbox_meters-27-4003f34e, and AGV_with_bbox_meters-2-03c38699. Final support relations are Box.a.8 -> Pallet.a.4 and IndustrialPart.k.18 -> Conveyor.c.1, followed by batch set_support/check_support, two support-suggestion moves, collision-suggestion moves, support recheck, final collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 8493189c is not directly LoRA-ready because the pasted material is not a legal JSON array, repairs collision before support setup, oscillates IndustrialPart.l.1 between support and collision suggestions, calls replace_instance late, and never reaches simulate_step/save_scene_usd. The original consolidated seed using Workbench.c.9, IndustrialRobot.a.2, Conveyor.b.4, Box.a.8, and IndustrialPart.l.1 hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task Workbench/IndustrialRobot/Conveyor/Box/IndustrialPart replacement seed while preserving the user command semantics. Final support relations are IndustrialPart.l.1 -> Workbench.a.5 and Box.a.5 -> Conveyor.b.3, followed by batch set_support/check_support, support-suggestion moves, collision-suggestion moves, support recheck with one support move, final collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 4c29da48 is not directly LoRA-ready because the source continuation repairs collision before support setup, alternates support/collision repair for Box.a.7_1 -> Pallet.a.2, skips simulate_step, saves with include_physics=true, and still has non-authoritative support_state at save. The original first-two seed is structurally usable but hung in Isaac check_support, so the final regenerated LoRA candidate uses a stable same-task rack picking replacement seed preserving the command semantics. Final support relations are Box.a.1 -> Rack.a.1 and Box.a.7 -> Pallet.a.3, followed by batch set_support/check_support, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment c15fb3bd is not directly LoRA-ready because the source continuation repairs collision before support setup, checks only IndustrialPart.k.17 -> Conveyor.c.1 before save while leaving Box.a.8 -> Workbench.b.7 unchecked after support setup, skips simulate_step, and saves with include_physics=true. The first two records are structurally usable as a generator seed, and the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.k.17 -> Conveyor.c.1 and Box.a.8 -> Workbench.b.7, followed by batch set_support/check_support, support-suggestion moves, collision-suggestion moves, repeated support/collision rechecks, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment fecb5820 is not directly LoRA-ready because the source continuation checks and repairs collision before support setup, validates only Box.a.7 -> Rack.b.1 after registering two support relations, skips simulate_step, and saves with include_physics=true. After merging IndustrialRobot/Pallet/IndustrialPart/Rack/Box placements into the second seed record, the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.n.1 -> Pallet.a.4 and Box.a.7 -> Rack.b.1, followed by batch set_support/check_support, one support-suggestion move for Box.a.7, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 45ed4337 is not directly LoRA-ready because the source continuation includes a move during the placement phase, runs check_support before set_support, validates only one relation after registering two support relations, skips simulate_step, and saves with include_physics=true. After merging Conveyor/IndustrialRobot/Workbench/Box/IndustrialPart placements into the second seed record and folding the robot clearance move into the initial placement, the original assets converged under the fixed generator without replacement. Final support relations are IndustrialPart.l.18 -> Workbench.c.9 and Box.a.7 -> Conveyor.b.4, followed by batch set_support/check_support, two support-suggestion moves, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Attachment 5c41c556 is not directly LoRA-ready because the source trace repeatedly checks and repairs collisions, never calls set_support/check_support, never reaches simulate_step, and never saves; the last available state is still in collision before the final move result. After merging Rack/Pallet/AGV/two Box placements into the second seed record and preserving later collision-separated positions, the original assets converged under the fixed generator without replacement. Final support relations are Box.a.7 -> Pallet.a.2 and Box.a.7_1 -> Rack.a.3, followed by batch set_support/check_support, one support-suggestion move for Box.a.7_1, collision clear, simulate_step(write_back=true), and save_scene_usd(include_physics=false).
- Batch source planner_toolcall_sft_preview.json contains 109 episodes when split on current_step == 1.
- Batch support relation extraction should prefer explicit trace actions, in order: set_support first, then check_support, then command phrases such as "box is supported by pallet" and nearest matching placed instances. If command support phrases cannot be fully matched to placed instances, the episode should be failed as support_unresolved rather than guessed.
- Batch generator seeds should preserve record 1 retrieve_asset and replace record 2 with a consolidated batch place_instance using all source place calls, folding later observed/moved positions when available.
- The batch tool must not perform second-round replacement; unresolved or non-convergent episodes are written to failed_episodes.json with needs_manual_review=true.
- /home/simple/isaac_env/bin/python is a venv interpreter symlink and should not be Path.resolve()'d before subprocess invocation, otherwise the subprocess uses system Python and can miss venv-only packages.
- Full seed-only batch scan built seeds for 106 of 109 episodes. Episodes 53, 57, and 107 are support_unresolved because the source material lacks enough matching placed support child instances for the user command.
